<%= form_with(model: user_listing, local: true) do |form| %>
  <% if user_listing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_listing.errors.count, "error") %> prohibited this user_listing from being saved:</h2>

      <ul>
      <% user_listing.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
